# Handoff — mise-en-place (2026-08-25)

Purpose: continue **without re-deriving context**. Read this first, then the plan
(`docs/plans/PLAN_recipe_app_foundation.md`) and `docs/ROADMAP.md`. The older
`HANDOFF_2026-08-23.md` holds the full GATE 0 / review history; this file is the **current
go-forward state**.

---

## 1. Where we are (one line)
GATE 0 passed, plan **approved and revised** (2026-08-25). **No app code yet.** We are
**setting up the WSL2 dev environment** so implementation of **Milestone 0 / T0.1** can begin.
Backend work is **mentor mode** — the human writes the C++, Claude guides + reviews.

## 2. What changed this session (2026-08-25) — a post-approval plan revision
A C++-experienced friend reviewed the ROADMAP; the human adopted two of his points. **Neither
touches the frozen D1 architecture**, so GATE 0's approval was **not** reset. A read-only
`workflow:plan-reviewer` pass over the diff returned **APPROVE, no blocking findings**.

1. **Build tool: CMake → plain GNU Makefile** (+ `pkg-config`). The build is Linux-only
   (WSL2 dev, Linux deploy), so CMake's cross-platform payoff is moot; a hand-written Makefile
   is simpler and keeps compile/link visible (learn-the-fundamentals goal). CMake stays a clean
   later switch if a Windows target is ever added.
2. **Thread pool moved to LAST in M0.** The `net` HTTP server is now **single-threaded through
   T0.8**; the thread pool became **new task T0.9** (right before the M0 review). SSL was
   already last (T0.8). De-risks the TSan/race learning curve. **T0.1–T0.8 numbers unchanged.**

Docker was discussed (friend suggested dropping it) → **deferred to M6**; dev Postgres stays in
Docker for now. Committed as `82a07c9` (`docs: switch M0 build to Makefile; defer thread pool
to T0.9`) and **pushed** to `origin/recipe-app-foundation`. The plan, ROADMAP, and the old
handoff's §3/§4 were all reconciled to the current stack. Full detail is in the plan's newest
**Reviewer notes** entry.

## 3. Environment setup — IN PROGRESS (this is the current task)
Target dev env = **WSL2 Ubuntu**, repo in the **WSL filesystem** (`~/dev/mise-en-place`), NOT
`/mnt/e/...`. Probed the machine this session:

**Present:** Windows 11 (build 26200); **WSL2 v2.6.1** (modern, kernel 6.6); **Docker Desktop
28.5.1** (WSL2 backend); GitHub remote `origin` = `git@github.com:AbsolutJin/mise-en-place.git`.

**Missing / to do (in order):**
1. **Install an Ubuntu dev distro** — the ONLY WSL distro is `docker-desktop` (Docker's internal
   appliance, no bash — not for dev). Chosen: **Ubuntu-24.04 LTS** (gcc 13 = full C++20; all
   four libs in apt). Run in a Windows terminal (interactive — creates a UNIX user/password):
   `wsl --install -d Ubuntu-24.04`
2. **Enable Docker Desktop → Settings → Resources → WSL Integration** for **Ubuntu-24.04** (so
   `docker` / `docker compose` work inside Ubuntu for the dev Postgres).
3. **Install toolchain + deps inside Ubuntu:**
   `sudo apt update && sudo apt install -y build-essential make pkg-config git gdb libpq-dev libssl-dev libutf8proc-dev catch2 postgresql-client clang`
   (`clang` isn't needed for T0.1 but is required at T0.2/T0.4 — libFuzzer is clang-only.)
4. **Clone the repo into WSL** at `~/dev/mise-en-place`.

**BLOCKER hit at step 4:** cloning via SSH failed — `git@github.com: Permission denied
(publickey)`. Cause: **Ubuntu has no SSH key registered with GitHub** (the Windows key doesn't
carry into the WSL distro). **Not yet resolved.** Options given to the human:
- **HTTPS clone** (`https://github.com/AbsolutJin/mise-en-place.git`) — instant if the repo is
  **public** (public/private status is UNKNOWN — the human hadn't answered yet).
- **SSH key in Ubuntu** (`ssh-keygen -t ed25519` → add the `.pub` to GitHub → `ssh -T
  git@github.com`) — matches the existing SSH `origin`, and is needed to **push** the human's
  C++ work later.
- **`gh` CLI** (`sudo apt install gh && gh auth login`) — sets up HTTPS auth for clone + push.
Recommendation: HTTPS-clone now to get moving; set up SSH keys (or gh) before the first push.

## 4. Next session — start here
1. **Run Claude from INSIDE Ubuntu, at `~/dev/mise-en-place`** — not this Windows session, not
   `/mnt/e/...`. Only there does the Bash tool run real Linux `gcc`/`make`/`apt`/`docker`.
   (Claude Code must be installed inside Ubuntu too: `which claude` — if absent,
   `curl -fsSL https://claude.ai/install.sh | bash`.)
2. **Finish setup** (whatever of §3 is unfinished — likely the clone/auth).
3. **Verify the toolchain** (the T0.1 gate):
   ```bash
   pwd                        # /home/<you>/dev/mise-en-place, NOT /mnt/e/...
   g++ --version              # 13.x, C++20-capable
   docker compose version     # Docker reachable from inside Ubuntu
   for lib in libpq openssl libutf8proc; do pkg-config --exists $lib && echo "OK $lib $(pkg-config --modversion $lib)" || echo "MISS $lib"; done
   ```
   (Note: `pkg-config --exists` is silent by design — it sets an exit code; the loop above makes
   the result visible. Three `OK` lines = clear for T0.1.)
4. **Start T0.1** — MENTOR MODE: the **human writes** the Makefile + `/backend/lib`
   (`net router json jsonschema db httpclient`) + `/app` + `/tests` skeleton + a Catch2 target
   + an ASAN+UBSAN config (`make asan`) + the dev `docker-compose` Postgres. Claude guides and
   reviews; Claude does **not** write the C++ and does **not** run `workflow:implementer` for
   C++ tasks. See the T0.1 spec in the plan and tick `docs/ROADMAP.md` as it lands.

## 5. Rules that still hold
- **Mentor mode (C++ backend):** human implements; Claude guides + reviews as a senior. Frontend
  (Angular) and docs/plan/config are exempt — Claude may write those. (Repo `CLAUDE.md`, local +
  gitignored; also in memory.)
- **D1 is FROZEN** — no framework / package-manager / from-scratch-scope pivots. A material change
  needs a plan revision + review, not silent edits. (Build tool + intra-M0 sequencing are NOT D1.)
- **Branch:** all work on `recipe-app-foundation`. Conventional Commits, **no attribution
  trailers** (user's global rule). `main` untouched; no PR opened.
- **Do NOT** use the user's `snacktrack` repo or `mealplan` plugin — out of scope.

## 6. Repo state
- Committed & pushed: all `docs/` (approved+revised plan, ROADMAP, both handoffs, 5 external
  reviews, reference captions, README, `.gitignore`). Latest = `82a07c9`. **No app code yet.**
- `origin/recipe-app-foundation` is up to date (this session pushed the pending commit).
