# Handoff — mise-en-place (2026-08-30)

Purpose: continue without re-deriving context. This is the current go-forward state. The
prior `HANDOFF_2026-08-25.md` holds the still-valid environment-setup detail; the older
`HANDOFF_2026-08-23.md` holds the full GATE 0 / review history.

## 1. Where we are (one line)
GATE 0 passed, plan approved. Still **no app code** — the real next step is unchanged from
2026-08-25: finish the WSL2 dev environment, then start Milestone 0 / T0.1 in mentor mode.
This session did a **docs pass only** (no code, no plan-substance change).

## 2. What changed this session (2026-08-30) — a docs de-slop + German switch
The human-facing docs were rewritten for a calmer, less decorated voice and switched to
**German**. Committed as `c09a47e` and pushed to `origin/recipe-app-foundation`.

- **New `docs/STYLE.md`** defines the approach and is the reference for future docs:
  form/structure only (substance untouched), one canonical source per fact (the plan owns
  stack/design; ROADMAP is progress; README is the door), no emoji, sparse emphasis, German
  with English technical terms kept.
- **`README.md`, `docs/ROADMAP.md`, and the plan's living spec** (Goal, D1–D6, Recipe format,
  Milestones M0–M6, Open questions) are now German and de-slopped.
- **Deliberately left as-is:** the plan's **frontmatter** (`approvals`/`status`), its
  **audit trail** (`## Reviewer notes` onward — an English record, like `docs/reviews/`), and
  the **jsonc `Recipe` block** (normative spec code). Security invariants, every task's
  `Verify`, review tags (e.g. `5th-review T2`) and all technical facts are unchanged.
- **Language decision:** the repo is treated as **private / human-is-sole-reader**, so
  reader-facing docs are German. Machine-facing docs (handoffs, reviews) stay English.

No D1 / plan-substance change, so GATE 0's approval is **not** affected.

## 3. Next session — start here (unchanged from 2026-08-25)
The docs work does not move implementation forward. Resume the environment setup exactly as
`HANDOFF_2026-08-25.md` §3–§4 describes:
1. Run Claude from **inside Ubuntu at `~/dev/mise-en-place`**, not this Windows session.
2. Finish the WSL setup (clone/auth was the open blocker — HTTPS clone or an Ubuntu SSH key).
3. Verify the toolchain (the T0.1 gate: `g++` C++20, `docker compose`, the three `pkg-config`
   libs).
4. Start **T0.1** in mentor mode (human writes the C++; Claude guides + reviews only).

## 4. Rules that still hold
- **Mentor mode (C++ backend):** human implements; Claude guides + reviews. Frontend, docs,
  plan and config are exempt — Claude may write those (this session's docs work was exempt).
- **D1 is FROZEN.** Docs form/wording is not D1; a material design change needs a plan revision.
- **Branch:** all work on `recipe-app-foundation`. Conventional Commits, **no attribution
  trailers** (subject stays English even though the docs are now German). `main` untouched.
- **New:** `docs/STYLE.md` governs the reader-facing docs — follow it when editing README /
  ROADMAP / plan, and keep those in German.

## 5. Repo state
- Committed & pushed: `c09a47e` (docs de-slop + German + STYLE.md) on top of the 2026-08-25
  state. `origin/recipe-app-foundation` is up to date. **No app code yet.**
