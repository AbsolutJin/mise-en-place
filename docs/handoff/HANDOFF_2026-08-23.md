# Handoff — mise-en-place recipe app (2026-08-23, updated 2026-08-24)

Purpose: give the **next session** everything it needs to continue this project without
re-deriving context. Read this first, then read the plan:
`docs/plans/PLAN_recipe_app_foundation.md`.

---

## 1. What this project is
A **single-user, self-hosted web app** to store, browse, and add recipes. Recipes can be
added via a **structured form** or by **pasting free text** (TikTok / Instagram caption,
website text), which is normalised into one **standardised JSON recipe format**. Recipes
render with optional images, an optional source link, and **macros per portion and per
100 g**. Macros come from **Open Food Facts** (search-and-pick per ingredient) with an
**LLM-estimate** button and always-editable manual override.

The user is **German** and cooks from German recipes — this drove the nutrition-source
choice (see §3, Nutrition).

---

## 2. Where we are in the workflow (IMPORTANT)
This project runs through the **`workflow` plugin** from the user's separate repo
`AbsolutJin/claude-tools` (a Claude Code plugin marketplace). Staged pipeline:
**understand → plan (GATE 0) → implement ⇄ test → review (GATE A) → document (GATE B) →
commit.**

Current stage: **GATE 0 PASSED — plan APPROVED. Ready to start IMPLEMENT (Milestone 1).**
- `understand` stage: **done**.
- `plan` stage: **complete.** `status: approved`; **both signatures dated
  `2026-08-24`** (`approvals.reviewer` + `approvals.human`). GATE 0 is closed.
- **The human approved (2026-08-24):** auth-ready schema with no auth this phase; all six
  milestones; VPN/basic-auth-over-TLS caveat accepted; the `macrosEstimated` "do both"
  honesty choice confirmed.
- **THREE deep external reviews** (all under `docs/reviews/`, each two independent passes)
  were folded in before sign-off. They **converged**: R1 = 3 blocking + 6 major; R2 = 1
  blocking + 5 major; R3 = **0 blocking + 1 major** (named this the exit point). Every
  external pass found blockers AFTER the workflow `plan-reviewer` said APPROVE, and some
  fixes spawned smaller ones — the workflow reviewer is weak on implementability depth +
  fact-checking (see [[external-review-before-signoff]] memory). Full trail in §5 + the
  plan's Reviewer notes (internal Rounds 1–12).
- **The PreToolUse gate now OPENS:** with the plan approved, edits to non-`docs/` files are
  allowed. Implementation code can be written for the first time.

### Next: IMPLEMENT stage — Milestone 1
- Follow the workflow's **implement** stage (skill/subagent `workflow:implementer`), **one
  task at a time** (T1.1 → verify → T1.2 → verify → …), with a `workflow:review` at the
  milestone boundary and `workflow:test` running each task's own verify step.
- **T1.1 first** — needs a working **C++ toolchain + CMake + vcpkg + Docker** on the machine;
  the first Drogon/vcpkg build compiles the whole dep tree and is **slow** (see Q8 — a vcpkg
  binary cache is planned in T6.1 but the very first build is unavoidably long). **Confirm
  the toolchain is installed before starting**, or make toolchain setup the first action.
- Keep all work on `recipe-app-foundation` (§7).

### Environment note (changed since the original handoff)
This session ran **locally on Windows** (repo at `E:\Dev\mise-en-place`, workflow plugin at
`E:\Dev\claude-tools\plugins\workflow`). Here the plugin's skills **ARE** invokable —
`Skill("workflow:plan")` and the `subagent_type: workflow:plan-reviewer` both work. All
GATE 0 rounds this session were run that way (real read-only `plan-reviewer` subagent), not
via a `general-purpose` Agent. (The earlier remote-env notes about `/home/user/claude-tools`
and manual SKILL.md following applied to a prior session only.)

### How to resume the workflow next session
1. Confirm the workflow plugin loads (skills listed: `workflow:plan`, `workflow:review`, …).
2. **Check for the second external review** (a new `docs/reviews/PLAN_REVIEW_*.md` or a file
   the user hands over). If present and not yet folded in: address it (planner edits only),
   re-run the workflow reviewer, then proceed.
3. Once the second review is addressed and the plan is reviewer-approved, **re-present the
   two sign-off questions** and get an explicit human yes.
4. On approval → stamp `approvals.human: <date>` and `status: approved` in the plan
   frontmatter → advance to the **implement** stage, starting **Milestone 1**.

### Do NOT
- Do NOT use the user's `snacktrack` repo or the `mealplan` plugin / any macro/mealplan
  skill — explicitly **out of scope** for this project.
- Do NOT push to any branch other than the designated one (§7).

---

## 3. Locked design decisions (agreed with the user at GATE 0)
Full detail is in the plan's D1–D6. Summary:

- **Architecture:** split, monorepo. `/backend` (C++) + `/frontend` (Angular) + `/docs` +
  `docker-compose.yml`.
- **Backend:** **C++ with the Drogon framework** — chosen deliberately **to learn C++**
  (user accepts the extra plumbing). CMake + vcpkg; `drogon[postgres]` feature required;
  `valijson` for JSON-Schema validation; libcurl (sync) for HTTP to OFF and the LLM.
  DB access via Drogon `DbClient` `execSqlSync`.
- **Frontend:** **Angular SPA** (chosen to learn a structured framework; Reactive Forms
  suit the dynamic ingredient form). Pin the Angular major version. Dev proxy + prod
  nginx `/api` reverse-proxy → **same-origin, no CORS**.
- **Database:** **PostgreSQL** (chosen over SQLite because the user may add auth /
  multi-user **later**). Migrations = plain SQL files + idempotent runner with a
  `schema_migrations` table.
- **Auth:** schema is **auth-ready** (a `users` stub table + nullable `owner_id` on
  recipes) but **NO auth is implemented this phase**. User confirmed this default.
- **Interchange format:** **JSON** is canonical; the **JSON Schema in `docs/` is the
  single source of truth**, mirrored by the C++ struct + Angular type, validated at every
  boundary. Carries a **`schemaVersion`** field. (A YAML export/import convenience could
  layer on later; JSON stays canonical.) Storage is relational Postgres, not a JSON blob.
- **Nutrition:** **Open Food Facts (OFF)** behind a `NutritionSource` interface (USDA left
  as a future secondary-source seam). **Search-and-pick per ingredient** (human in the
  loop, not fuzzy auto-match). **Live OFF API on first lookup, then cached in Postgres**
  (`foods` table keyed by barcode `code`; `ingredients.food_id` links to it) so repeat
  lookups need no network. OFF API contract is pinned (endpoint, descriptive
  `User-Agent`, 429/backoff). Per-100 g mapping pinned: `energy-kcal_100g` (else
  `energy_100g ÷ 4.184` — this avoids a kJ/kcal ~4.18× calorie bug), plus
  proteins/carbohydrates/fat `_100g`; products missing `*_100g` are not-pickable/flagged.
  ODbL licence (fine for personal use; a small attribution line shown in UI).
- **Macros:** stored per-serving + servings + total weight; **per-100 g is derived**
  (`perServing × servings ÷ totalWeightG × 100`). Shows **"—"** whenever weight is
  incomplete (any ingredient unpicked or any unit unresolved). Sources: OFF / LLM / manual,
  always overridable.
- **Paste parsing:** two engines behind a `RecipeParser` interface — `RuleBasedParser`
  (offline) and `LlmParser` (local LLM). User selects the engine; parsed result always
  shown in an **editable preview** before saving.
- **Local LLM:** Ollama. Env: `LLM_BASE_URL` (default `http://localhost:11434`), `LLM_MODEL`,
  optional `LLM_API_KEY`. **D3 defaults to Ollama's native `/api/chat` with `format`=schema**
  (more reliable than the OpenAI-compatible `/v1/chat/completions` + `response_format`, which
  some models ignore); the OpenAI surface is a configurable fallback. Off unless the engine
  toggle selects it. Output validated against the schema (our own validator) with fallback.
  **NOTE: the from-scratch pivot means the HTTP client is our own (T0.8, sockets+OpenSSL), not
  libcurl.**

---

## 4. Milestones (all six in scope this phase)
Each task in the plan has its own verification step. High level:
1. **Backend scaffold, schema, DB, browse API** (C++/Drogon, CMake+vcpkg, migrations,
   `Recipe` JSON Schema, CRUD, `GET /api/recipes` + `/:id`).
2. **Angular scaffold, browse/detail, structured form** (manual macros; dev proxy).
3. **Paste import** (rule-based + local-LLM engines, editable preview).
4. **Macros from OFF** (search-and-pick, `foods` cache, unit→gram converter, macro engine,
   `POST /api/macros/compute`, LLM-estimate).
5. **Media, polish, search** (image upload; title search + tag/favorites/macro filters + sort).
6. **Deployment & docs** (multi-stage Dockerfile, nginx, docker-compose, `.env.example`).

---

## 5. Plan review trail (GATE 0 reviewer, read-only subagent)
- **Round 1** — original TS/SvelteKit stack: APPROVE, 8 non-blocking (folded in).
  Superseded when the stack changed.
- **Round 2** — after switch to Angular + C++/Drogon + Postgres: **CHANGES REQUIRED**,
  4 blocking (macro-compute ordering; no JSON-Schema validator; CORS/dev-proxy; vcpkg
  `drogon[postgres]`) + 10 non-blocking. All fixed; confirm pass = APPROVE.
- **Round 3** — after nutrition switch to Open Food Facts: **CHANGES REQUIRED**, 3
  blocking (OFF API contract; per-100 g mapping + kJ/kcal bug; broken total-weight
  invariant) + 6 non-blocking. All fixed; confirm pass = **APPROVE**.
- **Round 4** — re-confirm on this session's deltas (D4 parser scope, D3 LLM policy,
  milestone order): **CHANGES REQUIRED**, 1 blocking (Goal still listed "website text"
  as in-scope, contradicting the D4 scope lock) + 4 non-blocking (out-of-scope list;
  undefined LLM fallback; illustrative model tags; `LLM_MODEL` unset behaviour). All fixed.
- **Round 5** — diff-only confirm: **APPROVE**. Briefly stamped `2026-08-24`, then
  **rolled back** (see the external review below).
- **External review** (`docs/reviews/PLAN_REVIEW_2026-08-24_external.md`, two independent
  passes) — **CHANGES REQUIRED**: 3 blocking (B1 storage shape undecided; B2 no HTTP mock
  seam; B3 UTF-8/emoji parsing), 6 major (OFF rate-limit factual error + legacy endpoint;
  per-100 g "—" for real recipes; `execSqlSync` event-loop claim; OFF whole-food data
  quality; Ollama structured-output path; unauth security surface), 10 quality. **Two
  human decisions taken:** per-100 g → add a piece-weight/spoon table; scope → keep all six.
  All folded into the plan.
- **Round 6** — workflow reviewer re-run over the fold-in: **CHANGES REQUIRED**, 1 blocking
  (`food_id`/`foods` FK sequencing between T1.2 and T4.1) + 5 non-blocking (`foods` PK vs
  `foodId` type; `group` reserved word; `HttpClient` name collides with Drogon; T4.3 fake
  seam; missing `DELETE` endpoint). *(Numbered Round 7 in the plan's own notes.)*
- **Round 7/8** — fixes applied, diff-only confirm: **APPROVE**, briefly stamped, then
  **rolled back again** (2nd external review below).
- **2nd external review** (`docs/reviews/PLAN_REVIEW_2026-08-24_external-2.md`, two passes) —
  **CHANGES REQUIRED**: confirmed the 1st-review fixes were genuinely in the body, then found
  1 blocking (PUT child-table update semantics — would drop `food_id` picks), 5 major (2
  introduced BY the piece-weight fix: macro-honesty; OFF endpoint/`product_quantity`; +
  advisory-lock unsound over the pool; no API error contract; no list pagination), 6 minor.
  All folded in.
- **Round 9** — workflow reviewer over the 2nd fold-in, aimed at implementability depth:
  **CHANGES REQUIRED**, 1 blocking (`macrosEstimated` had no storage column → couldn't
  round-trip) + 4 non-blocking (migration-lock phrasing; 429-vs-cache; REST surface;
  macroSource lossy-accepted). All fixed.
- **Round 10** — diff-only confirm: **APPROVE**, zero findings.
- **3rd external review** (`docs/reviews/PLAN_REVIEW_2026-08-24_external-3.md`, two passes) —
  **APPROVE with findings: 0 blocking** (first time), 1 major (`macrosEstimated` didn't cover
  the LLM path), 8 minor (incl. two real implementability gaps — unauthored macro schema;
  uploaded-file GC). Named this the exit point. All folded in.
- **Round 11/12** — workflow reviewer over the 3rd fold-in + confirm: **APPROVE**, only 2
  non-blocking notes (LLM-unreachable status; Draft-7 `definitions` vs `$defs`), also folded.
- **GATE 0 CLOSED (2026-08-24):** `approvals.reviewer` + `approvals.human` both dated;
  `status: approved`. Human decisions captured: auth-ready-no-auth, six milestones,
  TLS caveat, `macrosEstimated` "do both".

---

## 6. Open-point discussion (GATE 0) — progress

### RESOLVED (folded into the plan)
1. **Exact `Recipe` JSON fields — LOCKED.** The user chose to lock the full field list now
   (option A) rather than defer to T1.2. Validated against **three real TikTok captions**
   (2 German, 1 English), now saved as `docs/reference/example-recipes.md`. Final format is
   the **locked field list** in the plan ("Standardised recipe format"). Key decisions:
   - **Added:** `tags[]` (absorbs category/cuisine; user-curated, not scraped from hashtag
     walls), `prepTimeMin`/`cookTimeMin`, `notes` (free-form; also holds storage/reheating
     blocks), `favorite` (boolean, chosen over a numeric rating).
   - **Ingredient tweaks:** `quantity` **and** `unit` are both nullable (→ "to taste" /
     garnish rows), a nullable `group` label per row kept flat (grouped sections like "Für
     die Sauce"), and a **language-neutral unit vocabulary** (g/kg/ml/l/Stück/TL≈tsp/EL≈tbsp/…).
   - **Dropped:** `difficulty`, `yield` (→ numeric `servings`), numeric `rating` (→ `favorite`).
   - **Steps** are flat/ordered and may be empty (video-only captions). Caption-provided
     macros fold into `macroSource: "manual"` (vetted in the editable preview).
   - `schemaVersion` keeps any parser-driven refinement a **versioned migration, not a
     redesign** — the user explicitly wants to revisit structure while building the parser.
2. **Browsing/search scope — LOCKED** (plan T5.2): title substring search; **tag filter**
   (multi-select, AND); **favorites-only** toggle; macro filters **`minProtein` +
   `maxCalories`**; sort by newest (default) / title / highest-protein. Full-text over
   ingredients/steps deferred (needs Postgres FTS).
3. **Paste parser depth — LOCKED** (plan D4 + T3.1): rule-based parser is the
   **best-effort primary** engine (fully handles the pinned caption patterns, works
   offline); `LlmParser` is the **fallback** for messy captions. **Scope this phase =
   social captions only** (TikTok/Instagram). Pasted website text and URL/HTML
   (schema.org JSON-LD) are **out of scope** — a clean later addition behind the same
   `RecipeParser` interface.
4. **LLM specifics — LOCKED** (plan D3): **no translation** — LLM parses captions into
   schema JSON preserving the original language (German stays German); OFF search handles
   German terms. Model is **not hard-coded** — `LLM_MODEL` picks it at runtime, so
   switching is an env change + restart (model must be pulled in Ollama), no rebuild.
   Documented **reference default `qwen3.5-9b`** (fast, best-family German), quality
   option **`qwen3.8-27b`**, and **Gemma 4 27B (Q4_K_M)** noted as a JSON-adherence
   alternative. JSON reliability comes from structured-output mode + `valijson` + fallback,
   not model obedience. (User runs Qwen locally; confirmed 2026 web check.)

5. **Milestone ordering / thin-slice — LOCKED:** keep the M1→M6 order as written; deploy
   last. No reorder. Rationale: M1+M2 already form the first end-to-end vertical slice
   (add via form → store in Postgres → browse/view), so the core loop lands at end of M2
   and later milestones enrich it; containerization stays consolidated at M6. User chose
   the simplest path over pulling Docker Compose forward.

### STILL TO DISCUSS (pick up here next session)
6. **Frontend/backend structure detail** — folder layout and how much per-file detail the
   user wants written into the plan before coding (the workflow can also flesh this out at
   the start of each milestone during implement). Minor — can be settled at GATE 0 or
   deferred into each milestone's implement step.

When resolved (or explicitly deferred), get the explicit GATE 0 human approval and
proceed to Milestone 1.

---

## 7. Repo / git facts
- **Working repo:** `AbsolutJin/mise-en-place` (this repo). Near-empty at start (README).
- **Designated branch (develop + push here ONLY):** `recipe-app-foundation`.
  `main` is untouched.
- **What's committed so far:** only `docs/` — the **approved** plan
  (`docs/plans/PLAN_recipe_app_foundation.md`), the reference captions
  (`docs/reference/example-recipes.md`), **all three** external reviews
  (`docs/reviews/PLAN_REVIEW_2026-08-24_external{,-2,-3}.md`), and this handoff.
  **No app code yet** — Milestone 1 is the first implementation work.
- **No PR has been opened** (user hasn't asked for one).
- **Commit style: Conventional Commits, NO attribution trailers.** Per the user's global
  `CLAUDE.md`, commits use the configured git identity with **no `Co-Authored-By` / AI /
  tool trailers** (this overrides any harness default that adds them). This session's
  commits follow that.

---

## 8. Quick-start checklist for the next session
- [ ] Confirm the `workflow` plugin loads (skills `workflow:plan`, `workflow:review`,
      `workflow:implementer` / `subagent_type: workflow:implementer` are available locally).
- [ ] Read the **approved** `docs/plans/PLAN_recipe_app_foundation.md` and this handoff.
      (All three external reviews under `docs/reviews/` are folded in — read only for depth.)
- [ ] **GATE 0 is CLOSED** — plan `status: approved`, both signatures dated `2026-08-24`.
      No re-planning, no re-approval. Do NOT deviate from the approved plan (global CLAUDE.md
      planning gate); a material change means a new plan revision + re-review, not silent edits.
- [ ] **Dev environment = WSL2 (Ubuntu)** (decided 2026-08-24) — develop C++ inside WSL to
      match the Linux/Docker deploy target (dev/prod parity; smoother Drogon/vcpkg/Docker).
      **Clone the repo into the WSL filesystem** (`~/dev/mise-en-place`), NOT `/mnt/e/...`
      (cross-FS builds are slow). Editing via VS Code WSL remote / CLion.
- [ ] **Verify the C++ toolchain first (inside WSL):** CMake + a C++17/20 compiler (gcc/clang)
      + vcpkg + Docker. The first Drogon/vcpkg build is slow (whole dep tree). If missing,
      toolchain setup is the first action.
- [ ] **Start IMPLEMENT — Milestone 1, task T1.1**, via the workflow's implement stage
      (`workflow:implementer`, one task at a time → its verify step → next task; review at the
      milestone boundary). Milestones/tasks are in the plan (T1.1–T6.2).
- [ ] Keep all work on `recipe-app-foundation`. Commit per-task or per-milestone,
      Conventional Commits, no attribution trailers (§7).
